default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  url: <%= ENV["DATABASE_URL"] %>

development:
  <<: *default
  database: ecp_development

test:
  <<: *default
  database: ecp_test

production:
  primary: &primary_production
    <<: *default
    database: ecp_production
  cache:
    <<: *primary_production
    database: ecp_production_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    database: ecp_production_queue
    migrations_paths: db/queue_migrate
