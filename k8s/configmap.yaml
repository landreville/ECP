apiVersion: v1
kind: ConfigMap
metadata:
  name: ecp-config
  namespace: ecp
data:
  RAILS_ENV: "production"
  RAILS_LOG_TO_STDOUT: "true"
  RAILS_SERVE_STATIC_FILES: "false"
  RAILS_MAX_THREADS: "5"
  CORS_ORIGINS: "*"
  PORT: "3000"
